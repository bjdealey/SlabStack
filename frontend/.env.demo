# Build settings for the GitHub Pages demo: `npm run build:demo`.
# No secrets here — VITE_ variables are compiled into the public bundle.

# Answer API calls in the browser instead of over the network.
VITE_DEMO=true

# GitHub project Pages serve from /<repo>/, so assets need that prefix.
VITE_BASE=/SlabStack/
